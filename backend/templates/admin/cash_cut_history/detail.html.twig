{% extends '@EasyAdmin/page/content.html.twig' %}

{% block content_title %}
    Detalle del corte de caja
{% endblock %}

{% block main %}

    <div class="mb-4">
        <a
            href="{{ ea_url()
                .setRoute('admin_cash_cuts_history')
            }}"
            class="btn btn-secondary btn-sm"
        >
            Volver al historial
        </a>
    </div>

    <div class="card mb-4">
        <div class="card-body p-4">

            <div class="d-flex justify-content-between align-items-start mb-4">
                <div>
                    <h5 class="card-title mb-1">
                        Corte #{{ cashCut.id }}
                    </h5>

                    <div class="text-muted">
                        {{ cashCut.closedAt|date('d/m/Y H:i') }}
                    </div>
                </div>

                <div class="text-end">
                    <div class="text-muted small">
                        Usuario
                    </div>

                    <strong>
                        {{ cashCut.user.username }}
                    </strong>
                </div>
            </div>

            <div class="row g-3">

                <div class="col-md-3">
                    <div class="border rounded p-3 h-100">
                        <div class="text-muted small mb-1">
                            Total recaudado
                        </div>

                        <div class="fs-5 fw-bold">
                            ${{ cashCut.totalAmount|number_format(2, ',', '.') }}
                        </div>
                    </div>
                </div>

                <div class="col-md-3">
                    <div class="border rounded p-3 h-100">
                        <div class="text-muted small mb-1">
                            Comisión del usuario
                        </div>

                        <div class="fs-5 fw-bold">
                            ${{ cashCut.userCommissionAmount|number_format(2, ',', '.') }}
                        </div>

                        <div class="text-muted small">
                            {{ cashCut.userCommissionPercentage|number_format(2, ',', '.') }}%
                        </div>
                    </div>
                </div>

                <div class="col-md-3">
                    <div class="border rounded p-3 h-100">
                        <div class="text-muted small mb-1">
                            Total a retirar
                        </div>

                        <div class="fs-5 fw-bold">
                            ${{ cashCut.amountToWithdraw|number_format(2, ',', '.') }}
                        </div>
                    </div>
                </div>

                <div class="col-md-3">
                    <div class="border rounded p-3 h-100">
                        <div class="text-muted small mb-1">
                            Abonados cobrados
                        </div>

                        <div class="fs-5 fw-bold">
                            {{ cashCut.paymentsCount }}
                        </div>
                    </div>
                </div>

            </div>

        </div>
    </div>

    <div class="card">
        <div class="card-body p-4">

            <h5 class="card-title mb-3">
                Cobros de abonados incluidos en el corte
            </h5>

            <table class="table datagrid table-hover mb-0">
                <thead>
                    <tr>
                        <th>Fecha de pago</th>
                        <th>Abonado</th>
                        <th>Cliente</th>
                        <th class="text-end">Monto</th>
                    </tr>
                </thead>

                <tbody>
                    {% for payment in payments %}
                        <tr
                            role="button"
                            style="cursor: pointer;"
                            onclick="window.open('{{ ea_url()
                                .setRoute('admin_customer_detail_view', { customerId: payment.customer.id })
                            }}', '_blank')"
                        >
                            <td>
                                {{ payment.paidAt|date('d/m/Y H:i') }}
                            </td>

                            <td>
                                {{ payment.customer.subscriberNumber }}
                            </td>

                            <td>
                                {{ payment.customer.fullName }}
                            </td>

                            <td class="text-end">
                                ${{ payment.amount|number_format(2, ',', '.') }}
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="4" class="text-center text-muted">
                                Este corte no tiene pagos asociados.
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>

        </div>
    </div>

{% endblock %}